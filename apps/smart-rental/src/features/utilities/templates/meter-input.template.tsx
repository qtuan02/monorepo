// Placeholder until the slice is ported: the screen's heading and nothing else.
export default function MeterInputTemplate() {
  return (
    <h1 className="text-2xl font-bold tracking-tight">Nhập chỉ số điện nước</h1>
  );
}
