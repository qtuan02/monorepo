// Placeholder until the slice is ported: the screen's heading and nothing else.
export default function ReconciliationTemplate() {
  return (
    <h1 className="text-2xl font-bold tracking-tight">Đối soát chi phí</h1>
  );
}
