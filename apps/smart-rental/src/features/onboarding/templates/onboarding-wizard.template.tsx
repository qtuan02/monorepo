// Placeholder until the slice is ported: the screen's heading and nothing else.
export default function OnboardingWizardTemplate() {
  return <h1 className="text-2xl font-bold tracking-tight">Chào mừng!</h1>;
}
