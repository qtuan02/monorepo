import NotFound from "~/components/exception/not-found";

/** The localized 404, rendered inside the shell so the way out is still there. */
export default function NotFoundPage() {
  return <NotFound />;
}
