/**
 * The teal → lighter-teal gradient every "own"/primary surface in this
 * feature shares — own message bubbles and the composer's Send button
 * (design brief §10 row 2: `--primary` is the "what to do" accent).
 */
export const PRIMARY_GRADIENT_CLASSNAME =
  "bg-gradient-to-br from-primary to-[oklch(from_var(--primary)_calc(l+0.14)_c_h)]";
